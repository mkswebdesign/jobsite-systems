{
  "name": "scroll-progress",
  "assets": {
    "css": ["scroll-progress.css"],
    "js":  ["scroll-progress.js"]
  },
  "config": {
    "meta": [
      {
        "name": "aed:scroll-progress",
        "default": "on",
        "required": true,
        "description": "Master switch. Set content to 'on'/'true'/'1' to enable. Per-page opt-out: set data-aed-scroll-progress='off' on the <html> element.",
        "attrs": {
          "data-thickness": { "type": "string", "default": "", "enum": ["thin", "thick", ""], "description": "Bar weight. Empty string = default." }
        }
      }
    ]
  }
}
