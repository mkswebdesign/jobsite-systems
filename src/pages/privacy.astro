---
import { getEntry } from 'astro:content';
import LegalPage from '../builder/components/LegalPage.astro';

const entry = await getEntry('pages', 'privacy');
if (!entry) throw new Error('pages/privacy entry not found');
---

<LegalPage data={entry.data} />
